/*
    This class CoffeeSelection is the simple enum providing a set of predefined
    values for different kinds of coffees.
 */

public enum CoffeeSelection {
    FILTER_COFFEE, ESPRESSO, CAPPUCCINO;
}
